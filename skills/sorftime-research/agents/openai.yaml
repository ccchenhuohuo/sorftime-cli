interface:
  display_name: "Sorftime Research"
  short_description: "用本地 sorftime CLI 查亚马逊大盘、商品、关键词与配额，先说成本再花配额"
  default_prompt: "Use $sorftime-research to answer this Sorftime question through the local sorftime CLI, confirm the request-quota cost before spending, never spend Coin, and keep evidence and interpretation boundaries."

policy:
  allow_implicit_invocation: true
